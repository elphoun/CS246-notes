**Constants** are an important data type that initializes a variable that can't be mutated
* This helps to catch errors and should be defined as much as possible.
```
const int passingGrade = 50; // must be initialized
```